var name = "";
var fullName = name || "M A Noman";// will check the name has any value or not if not then store the second value 
var ok = true;
ok && console.log("Ok");//will check the value of the first half is true if then execute second half else will not execute the second half
